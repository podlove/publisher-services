{
  "packages": ["nodejs@latest", "elixir@latest", "erlang@latest"],
  "shell": {
    "init_hook": [],
    "scripts": {
      "install:client": ["cd client", "pnpm install"],
      "install:server": ["cd server", "mix deps.get"],
      "install": ["devbox run install:client", "devbox run install:server"],
      "server": ["cd server", "mix phx.server"],
      "client": ["cd client", "pnpm run dev"]
    }
  }
}
